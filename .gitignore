# Regenerated by Algorithm.java (deterministic given the fixed seeds) — too
# large for GitHub's 100MB per-file limit (276MB).
Data/dataset.csv

# Per-patient label export (exportPatientLabels) — same reason, 342MB.
Data/labels_drift_0.0.csv
